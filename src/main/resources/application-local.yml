server:
    port: 8080
spring:
    main:
        allow-bean-definition-overriding: true
    datasource:
        url: jdbc:mysql://localhost:3307/bankbox?autoReconnect=true&useSSL=false
        username: bankbox
        password: bankbox

    jpa:
        hibernate:
            ddl-auto: update
        database-platform: org.hibernate.dialect.MySQL8Dialect
